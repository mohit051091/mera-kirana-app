# Discovery Assumptions

1. **Meta App Setup:** Assumed that the Meta Commerce Catalog exists or will be created manually by the user following our setup instructions.
2. **PostgreSQL Credentials:** Assumed that Railway database environment variables will be injected or defined in the `.env` file on deployment.
3. **Admin Authentication:** Assumed that basic dashboard operations are local/unauthenticated for this stage of development (default, not confirmed).
